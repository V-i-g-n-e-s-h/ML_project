# ML_project

git clone <repo-url>
cd ML_project

python -m venv venv
# On Ubuntu
source venv/bin/activate  
# On Windows: 
venv\Scripts\activate

pip install -r requirements.txt

streamlit run app.py